# WindowsChangeCase
stupid program to avoid silly DOS scripts

This program reads either a command line argument or stdin, 
converts it to either upper or lower case (per -l or -U)
and writes to stdout.
